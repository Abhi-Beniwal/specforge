export const metadata = {
  title: "SpecForge — AI Requirements Engineering",
  description: "Multi-agent AI system that debates your product idea and generates a production-ready SRS",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="anonymous" />
      </head>
      <body>{children}</body>
    </html>
  );
}